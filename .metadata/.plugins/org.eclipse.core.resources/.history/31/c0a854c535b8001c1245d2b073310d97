/*
 * validaciones.c
 *
 *  Created on: 8 abr. 2022
 *      Author: BETO
 */

//validacion kilometros
//No puede validar datos alfabeticos por falta de comprension de vectores
#include <stdio.h>

int valKilometros(int kilometros)
{

		while(kilometros <= 0){
			printf("\nError! Reingrese los kilometros: ");
			fflush(stdin);
			scanf("%d", &kilometros);
		}
		return kilometros;


}

float valPrecioLatam(float precioLatam)
{
	int continuar;
	while(precioLatam < 0){
		if(precioLatam <= 0){
			printf("Esta ingresando un precio NO valido para calcular. \n");
			printf("Desea reintentar...? (1-SI/0-NO)");
			scanf("%d", &continuar);
			if(continuar == 1){
				printf("Ingrese el precio de vuelo de Latam: ");
				scanf("%d", &precioLatam);
			} else {
				precioLatam = -1;
			}
		}
	}
	return precioLatam;
}
