/*
 * validaciones.c
 *
 *  Created on: 8 abr. 2022
 *      Author: BETO
 */

void valOpcion(int opcion){
		int intento = 0;
		while(intento == 0){
			scanf("%d", &opcion);

			if(scanf("%d", &opcion)==0){
				intento++;
				fflush(stdin);
				printf("Error! Reingrese los kilometros: ");
				scanf("%d", &opcion);
			}
		}
}
